import 'package:flutter/material.dart';

class DetailScreent extends StatelessWidget {
  const DetailScreent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}