import 'package:flutter/material.dart';

class CartScreent extends StatelessWidget {
  const CartScreent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}